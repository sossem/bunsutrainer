# 분수 교실

초등학교 4~6학년의 개인 학습과 교실 전자칠판 수업을 위한 분수 학습 서비스입니다. Vanilla HTML/CSS/JavaScript로 구현되어 설치·빌드·회원가입 없이 사용합니다.

## 실행

`index.html`을 브라우저에서 열면 바로 실행됩니다. 일정한 주소에서 학습 기록을 관리하려면 프로젝트 폴더에서 로컬 서버를 실행하세요.

```powershell
python -m http.server 8000 --bind 127.0.0.1
```

브라우저에서 `http://localhost:8000`으로 접속합니다. 기존 `작업툴`의 로컬 서버 실행 배치 파일도 사용할 수 있습니다. 정적 호스팅에는 `index.html`, `css/`, `js/`, 학교 검색용 `elementaryschooldata.json`을 함께 올립니다. 학교 검색과 온라인 랭킹은 로컬 서버 또는 정적 호스팅 주소에서 사용하세요.

## 사용 방법

1. **개인 학습 / 전자칠판 수업**을 선택합니다.
2. 학년 → 단원 → 문제 유형 → 기초·기본·도전 → 5·10·20문제를 선택합니다.
3. 개인 학습은 답을 입력하고 확인합니다. 전자칠판 수업은 학생들이 공책에 푸는 동안 문제만 크게 제시합니다.

5단원·29유형을 제공하며 4학년의 **자연수 − 대분수**도 선택할 수 있습니다. 4·5학년 덧셈과 뺄셈 단원에서는 덧셈만 또는 뺄셈만 골고루 섞어 출제할 수도 있습니다.

### 전자칠판 수업

- 힌트 → 정답 → 풀이를 필요한 때에 공개합니다. 풀이는 이전/다음 단계로 넘깁니다.
- 다음 문제는 정답을 숨긴 상태로 시작하며, 이전 문제로 돌아가면 공개 상태를 기억합니다.
- **새 문제**는 현재 문제를 같은 유형으로 교체합니다. 마지막 문제의 수업 마무리 화면에서 **새 문제 세트**를 시작합니다.
- 수업 마무리 화면의 **이번 회차 문제와 정답 보기**를 펼치면 해당 세트의 문제와 정답을 함께 확인합니다.
- **유형 변경**으로 설정을 바꿀 수 있고, 시작하기 전 **진행 중인 문제로 돌아가기**로 돌아갈 수도 있습니다.
- FHD 1920×1080을 중심으로 설계했으며 전체화면을 지원합니다.

| 키 | 동작 |
|---|---|
| → / ← | 다음 / 이전 문제 |
| Space | 정답 공개 |
| H | 힌트 |
| S | 풀이 공개; 정답 공개 전이라면 먼저 정답 공개 |
| F | 전체화면 진입 / 종료 |
| T | 타이머 시작 / 일시정지 |
| R | 타이머 초기화 (일시정지 상태) |
| Esc | 브라우저 전체화면 또는 도움말 종료 |

입력 칸 및 도움말 안에서는 수업 단축키가 동작하지 않습니다.

### 타이머·나의 성장·학급 랭킹

- 설정의 **학습 도구**에서 타이머를 선택합니다. 기본은 사용 안 함이며 10·20·30·60초 또는 1~3600초를 직접 설정합니다. 시간 종료 후에도 계속 풀 수 있고 자동으로 다음 문제로 넘어가지 않습니다.
- 전자칠판에서는 시간 종료 시 정답 공개를 선택할 수 있습니다. 이미 정답을 공개했다면 현재 풀이·그림을 유지합니다. 문제 이동·설정·도움말·창 숨김 때 타이머를 멈추며 이전 문제로 돌아오면 남은 시간을 유지합니다. 시작 버튼으로 다시 진행합니다.
- 개인 학습은 점수·경험치·배지를 선택적으로 사용합니다. **나의 성장**에서 레벨·배지·연속 학습을 확인합니다. 정답 판정·첫 풀이 정답률과 성장 점수는 별도로 계산하며, 기존 기록을 열어도 보상을 다시 지급하지 않습니다.
- **학급 랭킹**에서 학교 검색 → 학년·반 선택 후, 개인 학습 설정에서 랭킹 참여와 온라인/데모를 선택합니다. 5문제 이상을 끝까지 학습한 새 세트만 반영합니다. 중간 종료·건너뛴 문제·오답 재학습은 학급 점수에 반영하지 않습니다.
- 랭킹 화면의 조회 버튼으로 이번 주/지난주 기록을 불러옵니다. 온라인 성장 점수, 이 기기의 데모, 예전 점수(조회 전용)는 분리됩니다. 온라인은 상위 50개 학급 및 우리 반 기록을 표시합니다. 서버 반영 실패 시 개인 기록에서 같은 학습을 재시도할 수 있습니다.
- 온라인 랭킹을 조회하거나 참여할 때만 Firebase SDK와 서버에 연결합니다. 온라인 이용에는 인터넷과 기존 Firebase 프로젝트의 읽기·쓰기 권한이 필요합니다. 서버 보안 규칙과 운영 쓰기 검증은 이번 로컬 검증 범위에 포함하지 않았습니다.

### 개인 학습과 기록

- 분수 답은 분자·분모, 대분수 답은 자연수·분자·분모를 입력합니다. 자연수 답은 자연수 칸만 입력해도 됩니다.
- Tab으로 이동하고 Enter로 확인합니다. 통분은 두 분수, 비교는 기호, 동치분수 빈칸과 공통분모는 자연수를 입력합니다.
- 4학년 계산은 같은 값의 분수를 정답으로 인정합니다. 기약분수를 요구하는 문제는 약분 여부도 확인합니다.
- 오답 뒤에는 힌트와 재도전을 제공합니다. 정답 또는 풀이를 먼저 확인한 문제는 재학습 대상이 됩니다.
- 정답률은 **전체 문제 중 첫 제출에 정답인 문제의 비율**입니다. 재도전 성공 개수는 따로 표시합니다. 힌트·그림을 참고한 첫 제출도 포함됩니다.
- 학습을 끝내거나 **여기까지 결과 보기**를 누르면 날짜·설정·정답·시도·시간·오답이 저장됩니다. 건너뛴 문제도 다시 풀 수 있습니다.
- `localStorage`에 최근 100회까지 저장합니다. 저장이 제한된 브라우저에서는 현재 창의 메모리에 보관하고 화면에 알립니다. 기록은 브라우저/접속 주소별로 분리되며 기기 간 동기화는 없습니다.

## 검증

계산·출제 테스트는 Node.js 20 이상에서 추가 패키지 없이 실행합니다.

```powershell
node --test tests/math.test.cjs tests/generator.test.cjs tests/storage-extension.test.cjs tests/extensions.test.cjs tests/session-extras.test.cjs
```

실제 브라우저 검증은 Windows에 설치된 Chrome, Python, `websocket-client`를 사용합니다. 테스트 도구만 해당 환경을 필요로 하며 서비스 자체에는 Python·Node가 필요하지 않습니다.

```powershell
python tests/browser_test.py --scenarios
python tests/extension_scenarios.py
python tests/ranking_scenarios.py
```

테스트는 전용 Chrome 프로필과 로컬 서버를 열고 종료합니다. 보고서와 화면 캡처는 `tests/artifacts/`에 생성됩니다. 일반 브라우저의 학습 기록은 건드리지 않습니다.

## 구조 및 보고서

- [기존 코드 분석과 설계](docs/IMPLEMENTATION.md)
- [구현 결과와 남은 개발 항목](docs/FINAL-REPORT.md)
- [계산·브라우저 검증 결과](docs/TEST-RESULTS.md)

계산은 `js/fraction.js`, 출제는 `js/problem-generator.js`, 저장은 `js/storage.js`, 분수 표시·시각 모델은 `js/ui.js`, 학습 흐름과 화면은 `js/app.js`가 담당합니다. `gamification.js`, `ranking.js`, `timer.js`는 `session-extras.js`와 `ranking-ui.js`를 통해 화면에 연결됩니다. `css/classroom.css`는 전자칠판 전용, `extensions.css`와 `ranking.css`는 선택 도구와 랭킹 스타일입니다.

기존 학교 데이터와 Git 작업 도구는 보존했습니다. 기존 전국 학급 점수는 조회 전용으로 보존하고 새 학습 성장 점수와 분리했습니다. 예전의 오답 3초 대기·자동 다음 문제 전환은 사용하지 않습니다. `codex-state.txt`, `git-diff.txt`, `git-status.txt` 및 테스트 산출물은 `.gitignore`로 커밋 대상에서 제외합니다.
